# React source

The supplied RIĦA landing page is a self-contained static HTML page, not a React application. The live GitHub Pages entry point is `../index.html`, so no React build step is required.
