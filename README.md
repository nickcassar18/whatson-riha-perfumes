# RIĦA Perfumes — GitHub Pages

This folder is ready to publish with GitHub Pages.

- `index.html` — top-level GitHub Pages entry file
- `support.js` — site support script
- `.nojekyll` — disables Jekyll processing

The current landing page is self-contained and keeps its existing embedded assets.
